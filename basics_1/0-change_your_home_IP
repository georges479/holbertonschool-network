#!/bin/bash
# This script updates /etc/hosts entries for localhost and facebook.com

# Add or update the localhost entry
sudo sed -i "s/^127\.0\.0\.1.*/127.0.0.2\tlocalhost/" /etc/hosts

# Add or update facebook.com entry
echo "8.8.8.8 facebook.com" | sudo tee -a /etc/hosts > /dev/null
